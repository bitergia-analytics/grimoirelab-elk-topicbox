name: Check changelog file included

on:
  pull_request:
    paths:
      - 'grimoire_elk_topicbox/**'

jobs:
  check-changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: bitergia/release-tools-check-changelog@master
